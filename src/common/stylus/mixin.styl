table($padding, $height, $backgroundColor)
  display flex
  padding $padding
  background-color $backgroundColor
  width 100%
  height $height
  box-sizing border-box
  justify-content space-between
  align-items center
  